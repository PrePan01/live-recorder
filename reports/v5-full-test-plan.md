# Live Recorder V5 全量测试计划（Web 端 / 桌面客户端 / 后端 / 全平台）

> 维护人：@QA · 状态：待 PrePan 审阅
> 依据：`v5-detailed-design.md`、`docs/api-contract.md`（v1.2–v2.4 及 v5 契约）、`reports/live-recorder-mvp-test-plan.md`、`reports/v5-p0-0-boot-matrix-test-cases.md`
> 范围：V5 全部功能 + 此前版本（v1–v4）回归，覆盖 Web 端、桌面客户端（macOS/Windows）、后端，以及测试层级与门禁。

---

## 1. 目标与范围

- **目标**：交付一份按测试行业规范（分层测试、可追踪、可验收、有门禁）的 V5 全量测试计划，作为 V5 验收与回归的基线。
- **覆盖对象**：
  - 后端服务（HTTP API + SSE + WebSocket 预览 + 侧车进程）
  - Web 管理端（浏览器访问 localhost）
  - 桌面客户端（Tauri：macOS .app/.dmg + Windows .exe/.msi）
  - 平台适配器（抖音、B 站）
- **平台**：macOS、Windows（桌面双平台 + 浏览器）；V5 已移除移动端三平台（见 `v5-detailed-design.md` 历史决策），本计划不再含手机矩阵，仅保留浏览器响应式相关抽查。
- **批次**：P0-0（零命令启动/端口）→ P0（1–6）→ Batch2（7–10）→ Batch3（11–15）。

## 2. 测试策略与层级（行业规范分层）

| 层级 | 覆盖内容 | 工具/方式 | 运行环境 |
| --- | --- | --- | --- |
| L1 单元测试 | 纯函数：URL 解析/去重、状态机迁移、文件命名、磁盘阈值、退避、邮件去重、命名规则、预测分档、搜索转义 | Vitest/Jest + fake timer | CI |
| L2 服务集成测试 | 调度轮询、重启恢复、并发控制、管线队列、上传队列、持久化、诊断单飞、端口回落、实例锁 | 可注入 fake 平台/录制器/磁盘/SMTP/时钟 + 临时数据目录 | CI |
| L3 API/契约测试 | 全部端点成功/错误响应、错误码、幂等、鉴权与敏感信息不泄漏 | Supertest/HTTP + 契约文件（v1.2–v2.4 + v5） | CI |
| L4 前端组件/UI 测试 | 状态色语义映射、空/错/弱网态、主题切换、深色对比、响应式 | Vitest + Testing Library / Playwright 组件 | CI |
| L5 Web E2E | 首次设置、房间 CRUD、录制生命周期、搜索/标签、自愈、通知、管线、导出、统计、直播墙 | Playwright（后端接可控 fake 服务） | CI（staging） |
| L6 桌面端 E2E/冒烟 | 安装、双击启动、单实例、端口占用恢复、托盘、目录选择、打开文件、原生通知、退出收束 | Tauri 真机冒烟 + 集成测试 | 发布前真机（macOS/Windows） |
| L7 平台冒烟 | 抖音/B 站各至少一个公开授权房间：探测、实际录制、integrity | 人工/受控环境 | 发布前 smoke |
| L8 性能/兼容回归 | 门禁性能用例 + 浏览器兼容 + 双平台安装包冷启动/重启/退出 | 见 §8 | 发布前 |

**测试金字塔原则**：L1/L2 为大宗（自动化、快、稳定），L3/L4 次之，L5–L8 为验证性/真机冒烟，不追求全自动化。

## 3. 测试环境与前提

### 3.1 测试替身契约（BE 必须可注入）

1. 平台状态/拉流适配器：可返回未开播、开播、受限、网络失败、断流、标题回退。
2. 录制器：可报告启动成功、写入失败、进度、结束、异常退出、0 字节。
3. 文件系统与磁盘：可模拟不可写、低空间、写入中断、目录越界。
4. SMTP/邮件发送器：记录调用、模拟超时/拒信/成功、预设回填。
5. OpenList/WebDAV 上传器：记录调用、模拟进度/失败/重试/取消/重复远端对象。
6. 时钟与调度器：可推进轮询、退避、去重窗口、定时计划（含跨天/时区），无真实等待。
7. 通知 API：可模拟权限被拒/已授权、投递成功/失败。
8. 原生能力（Tauri command）：`pickDirectory`、`openPath`、`notify`、服务健康，均可注入。

### 3.2 数据与安全前提

- 使用脱敏/可撤销的测试 Cookie；不得把凭据提交至仓库、日志、导出或告警。
- 所有敏感配置（Cookie、SMTP 密码、OpenList 令牌）读取接口只返回 `hasXxx`，不回显明文；导出/日志不得含密钥。
- 测试数据目录隔离；不污染用户真实录制目录与 live DB（回归用临时 worktree/副本库）。

## 4. P0-0 零命令启动与端口防冲突

详见 `reports/v5-p0-0-boot-matrix-test-cases.md`（BOOT-01~10），本计划引用并纳入门禁：

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| BOOT-01 | 全新安装/首次双击 | 无终端步骤、启动页 booting→ready、目录/通知权限失败可行动提示不白屏 |
| BOOT-02 | 冷启动与重启 | baseUrl/port 握手注入非写死；REST/SSE/预览同实例；重启不依赖旧固定端口 |
| BOOT-03 | 连续启动 10 次 | 单窗口/单 sidecar/单调度器/单锁；重复启动聚焦既有窗口 |
| BOOT-04 | 默认端口被占 | 自动换端口不影响外部进程；API/SSE/预览/录制可用；白名单随端口动态放行 |
| BOOT-05 | 全部候选端口不可用 | 进启动诊断，原因可读，安全重试，不要求 kill |
| BOOT-06 | 旧版本/残留状态 | 优先连接兼容版本；不兼容提示受控重启；残留经 PID/health 校验后清理 |
| BOOT-07 | 退出顺序 | 停止接收→收束调度/录制→删 ready/锁；SIGINT/SIGTERM 均走该顺序 |
| BOOT-08 | 正在录制时关闭窗口 | 不丢录；托盘驻留；明确退出二次确认后才停服务 |
| BOOT-09 | 设置「关闭即退出」 | 关闭窗口直接退出并安全收束 |
| BOOT-10 | 双平台安装包 | macOS/Windows 安装包冷启动、重启、退出均通过才算就绪 |

## 5. P0 核心功能测试（1–6）

### 5.1 自愈工作台

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| SH-01 | 诊断生成 | 故障发生后 5 秒内出现诊断；含错误码/严重度/建议 |
| SH-02 | 诊断单飞 | 同一 `recordingId+code` 不产生并发重复修复 |
| SH-03 | 动作幂等 | 重试/重新检测/忽略正确；无权或不可修复不误执行 |
| SH-04 | 生命周期 | open→processing→resolved/expired；历史保留 30 天；expired 后动作 409 |
| SH-05 | 修复联动 | 重试成功后状态改 resolved；触发 SSE `diagnostic:updated` |
| SH-06 | 筛选/详情 | 按严重度/房间/状态筛选；详情抽屉时间/错误码/建议正确；会改外部状态操作二次确认 |
| SH-07 | 磁盘/受限诊断 | DISK_SPACE_INSUFFICIENT、PLATFORM_ACCESS_RESTRICTED、SMTP_SEND_FAILED 分类正确、可行动 |

### 5.2 桌面通知与开播预测

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| NT-01 | 通知开关 | 开播/录制开始结束/失败/磁盘风险/上传失败可独立开关并持久化 |
| NT-02 | 测试通知 | `POST /notifications/test` 可验证权限与投递；失败有明确引导非静默 |
| NT-03 | 预测 | 近 7/30 天形成预测窗口；置信度高/中/低；无历史显示「数据不足」 |
| NT-04 | 通知安全 | 通知不含 Cookie/本地路径等敏感信息；权限被拒引导明确 |
| NT-05 | 桌面/浏览器通知 | Tauri 通知 API 与浏览器 Notification API 分别验证 |

### 5.3 标签分组与全局搜索

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| TG-01 | 标签 CRUD | 创建/改名/删除/颜色正确；删除仅解除关联不删房间/录像 |
| TG-02 | 批量加移标签 | 房间管理批量操作正确并持久化 |
| TG-03 | 分组/筛选 | 左侧/筛选区按标签分组正确 |
| TG-04 | 搜索命中 | `GET /search?q=&type=&tagId=&from=&to=` 房间/录制分组正确；类型跳转正确 |
| TG-05 | 搜索安全 | 空查询不发全量；特殊字符/LIKE 转义；超长 q 422；分页≤50；空结果键盘保护 |
| TG-06 | 搜索定位 | Rooms/History 消费 `?focus=id` 滚动定位+高亮 2s+URL 清理 |

### 5.4 监控总览重构（含手动录制）

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| MN-01 | 卡片/列表视图 | 状态点+文字传达状态；收藏置顶/排序/筛选/批量正确 |
| MN-02 | 状态语义 | 开播而自动录制关=「直播中，未自动录制」；live-idle 预览放开 |
| MN-03 | 手动录制 | 确认开播且未录制才可用；录制中切停止；操作 loading/反馈；未开播 409 |
| MN-04 | 实时性 | 状态/时长/导航录制数 SSE 实时；不靠长轮询伪造 |
| MN-05 | 隔离性 | 切页/关预览不停录制；预览=纯观看不落盘 |
| MN-06 | 响应式 | 卡片/列表桌面与窄屏不截断关键操作；窄屏侧栏展开 |

### 5.5 UI 视觉升级与深色模式

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| UI-01 | 主题切换 | 浅/深/跟随系统三态；设置即时预览；刷新保持；首屏无闪烁 |
| UI-02 | 对比度 | 核心页正文与关键控件满足 WCAG AA |
| UI-03 | 状态色 | 状态必须配文字/图标不只靠颜色；键盘焦点/禁用/加载/空态统一样式 |
| UI-04 | 响应式 | 桌面/移动断点无横向溢出；动效跟随系统偏好 |
| UI-05 | 设计令牌 | 组件用 CSS 变量/主题层，无散落硬编码色 |

### 5.6 桌面客户端（Tauri）与移动端适配

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| DC-01 | macOS | 安装、启动、通知、选择目录、打开文件跑通 |
| DC-02 | Windows | 同一协议下安装、启动、通知、选择目录、打开文件验证 |
| DC-03 | 最小原生能力 | `pickDirectory/openPath/notify/健康检查` 仅暴露必要能力 |
| DC-04 | 版本兼容 | 客户端/服务版本经 `GET /health` 校验兼容性 |
| DC-05 | 手机浏览器 | 查看状态/搜索/手动录制/停止可操作，无不可操作悬浮布局（仅抽查） |

## 6. Batch2 后处理与分发测试（7–10）

### 7. 后处理管线

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| PL-01 | 管线流程 | 校验→sidecar 元数据→封面帧→切片/合并→压缩/转封装→归档→DB 更新 |
| PL-02 | 并发控制 | 默认 N=2、FIFO、主录制优先；配置快照不追溯 |
| PL-03 | 失败保留 | 单步失败保留成功产物标记 partial/failed；不破坏原始录制；原文件始终可用 |
| PL-04 | 定向重试 | 只重跑失败步骤，不重跑成功步骤；`POST /recordings/:id/pipeline/retry` |
| PL-05 | 配置变更 | 改动仅影响后续录制；非法配置 422 |
| PL-06 | 封面 | `GET /media/cover/:recordingId` 404 回退占位图并沿用鉴权 |

### 8. 自定义命名规则

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| NM-01 | 预设/自定义 | `{room}/{platform}/{date}/{time}/{quality}/{roomId}` 模板生成可用文件名；默认 `{room}_{date}_{time}` |
| NM-02 | 安全处理 | 非法字符过滤、保留扩展名、长度截断、同名追加序号 |
| NM-03 | 边界 | 空值回退、超长、重名不导致录制失败 |
| NM-04 | 生效范围 | 修改只影响新录制；历史回放/归档/手动重命名不受影响 |

### 9. OpenList 自动上传

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| OL-01 | 配置/测试连接 | `GET/PUT /settings/openlist`、`POST /settings/openlist/test`；未配置不建任务有引导 |
| OL-02 | 上传 | 管线完成/用户指定触发；队列/进度/重试/取消；本地原文件保持不变 |
| OL-03 | 恢复/幂等 | 可恢复、可重试且不重复写入同一远端对象 |
| OL-04 | 安全 | 令牌不在接口/日志/导出泄露；`hasCredential` 只回布尔 |
| OL-05 | 失败联动 | 上传失败进入自愈工作台；`POST /uploads/:id/retry` |

### 10. 邮件通知简化

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| EM-01 | 预设/自定义 | 常用服务商预设自动填充主机/端口/加密；自定义可保存 |
| EM-02 | 测试邮件 | `POST /settings/email/test` 成功/失败反馈准确；保存前可测 |
| EM-03 | 回显安全 | 密码只写不回显；仅返回 `hasPassword`；导出不含密码 |
| EM-04 | 去重 | 同房同类事件 30 分钟去重；不同事件不互相抑制 |
| EM-05 | 失败隔离 | SMTP 失败不中断录制；失败提示字段级不含完整服务器响应 |

## 7. Batch3 效率与增强测试（11–15）

### 11. 多路直播墙

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| WL-01 | 布局 | 2×2/3×3；并发 4 路真实预览；默认静音；单路可取消静音 |
| WL-02 | 并发上限 | 第五路提示并允许替换；不拖垮已有画面；超过容量返回可识别错误码 |
| WL-03 | 隔离 | 关闭墙/切页不停止录制 |
| WL-04 | 异常恢复 | 加载失败显示重连按钮与原因；单格恢复 |
| WL-05 | 记忆 | 布局可记忆；移动端纵向列表/分页不硬塞 3×3 |

### 12. 定时录制计划

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| SC-01 | 周计划 | 按房间设周几+时间窗；启停/复制/查看下次执行 |
| SC-02 | 时区/跨天 | 时区默认本机；跨天时间窗解释正确；`computeNextRunAt` 时区感知 |
| SC-03 | 触发语义 | 时间窗到点检测；仅确认开播且允许自动录制才启动；离线不空录 |
| SC-04 | 防重 | 重复计划不并发重复录制；重启恢复正确；下次执行时间与实际一致 |

### 13. 统计看板

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| ST-01 | 指标 | 今日/本周/自定义区间：场次/时长/大小/成功率/平台分布/趋势 |
| ST-02 | 复算 | 指标可由录制列表复算一致 |
| ST-03 | 边界 | 时区边界、空区间、筛选组合（平台/标签/房间）正确 |
| ST-04 | 性能 | 大量历史数据有加载态且不阻塞监控实时更新；聚合服务端缓存 |

### 14. 录制备份与导出

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| EX-01 | 单场/批量导出 | 含原视频+sidecar+封面+manifest；可解压且文件完整 |
| EX-02 | manifest | 含 recordingId、文件哈希（SHA-256）、导出时间、应用版本；不含密钥 |
| EX-03 | 部分成功 | 缺失 sidecar/封面明确标记部分成功 |
| EX-04 | 取消/重试 | `POST /exports/:id/cancel` 不损坏源文件；`POST /exports` 失败可重试 |
| EX-05 | 包名唯一 | 重复导出生成可区分包名，不覆盖已有文件；可打开所在目录 |

### 15. 抖音标题回退加固

| 用例 | 场景 | 通过标准 |
| --- | --- | --- |
| DT-01 | 主源可用 | 主接口正常时不走回退 |
| DT-02 | 主源失败回退成功 | 仍正常录制；UI 显示标题来源非阻塞提示 |
| DT-03 | 全部失败 | 用安全占位标题；录制可继续；UI 不空白 |
| DT-04 | 分类正确 | Cookie 失效与真实接口结构变化分类正确，不误报「平台变动」 |
| DT-05 | 可追踪 | `titleSource/titleUpdatedAt/titleFallbackUsed` 可追踪 |

## 8. 性能与兼容性门禁

| 用例 | 门禁 |
| --- | --- |
| PERF-01 | 录制写入零阻塞（不阻塞调度/UI） |
| PERF-02 | 预览 4 路并发带降级；2–4 路压力不崩溃 |
| PERF-03 | 搜索响应（带索引+超时）在限值内 |
| PERF-04 | 统计聚合缓存命中；大量历史数据可加载 |
| PERF-05 | 前端首屏 ≤ 目标值（实测 357ms）；路由切换瞬时；bundle 0.45MB 懒加载无回归 |
| COMP-01 | 浏览器兼容：Chrome/Edge/Safari/Firefox 核心流程可用 |
| COMP-02 | macOS + Windows 安装包冷启动/重启/退出均通过 |

## 9. 缺陷门禁与阻塞定义

### 9.1 阻塞缺陷（Blocker，任一出现即阻止发布）

1. 任一目标系统无法完成首次配置、保存或重启恢复。
2. 已启用且实际开播的有效房间无法启动录制，或同场直播产生并行/重复录制。
3. 文件未写入指定目录、历史记录与实际文件不一致、无法定位失败原因、录制时长显示错误。
4. 磁盘阈值未阻止新录制（可能耗尽磁盘）或断流重试无限循环。
5. 正在录制时关闭窗口丢录；明确退出未二次确认即停服务。
6. 敏感配置（Cookie/SMTP 密码/OpenList 令牌）在接口、日志、导出或告警中泄漏。
7. 关键依赖不可替换，导致异常场景无法在自动化中稳定复现。
8. 端口占用时无法自动恢复或误杀外部进程。
9. 桌面客户端白屏（WebView 渲染失败）或服务启动诊断缺失。

### 9.2 严重/次要分级

- **Critical**：核心功能异常但可绕过（如单类通知失效、单平台受限但可降级）。
- **Major**：非核心功能缺陷，有可接受 workaround。
- **Minor**：文案/样式/非阻塞体验问题。

## 10. 完成门槛（Definition of Done）

- **提测门槛**：L1–L3 用例可注入替身可用；接口契约已冻结；P0 验收项均有对应用例。
- **批次出口**：该批 L1–L3 全绿 + L4/L5 轻量冒烟 + 定向回归通过，通知 PrePan 验证。
- **收口全量**：V5 全功能 + v1–v4 回归全绿；L8 性能/兼容 + L7 平台冒烟通过。
- **发布前**：macOS 与 Windows 最小 smoke 各通过一次；抖音/B 站各完成一次授权公开房间实际录制验证。
- **文档同步**：`docs/api-contract.md` 与测试用例一致；新增端点错误码入库（当前 19 码全集 + v5 新增码）。

## 11. 回归基线（v1–v4 存量功能）

| 板块 | 关键回归项 |
| --- | --- |
| 房间管理 | CRUD、启停、删除（保留历史）、收藏、重命名、批量/CSV、分页、添加显示名即时刷新 |
| 录制生命周期 | 检测 200/244ms、自动/手动录制、停止、断流续录、0 字节标记 failed、启动恢复遗留会话、并发上限 2、磁盘守卫 |
| 历史/回放 | 历史列表、筛选（quality/日期）、重命名同步文件、删除连带文件+容错、回放（FLV/MP4）可播放、时长显示正确 |
| 预览 | 中途加入 FLV 可初始化、4002、FLV 头跨录制重置、预览不落盘 |
| 设置 | 保存路径（目录树选择器）、主题、管线、通知、OpenList、邮件预设、导出/导入、Cookie 不回显、self-check |
| 通知/告警 | 邮件去重、磁盘风险、上传失败、SSE 事件 |
| 安全 | 配置导出无密钥泄漏、导入部分成功语义、绝对路径防穿越 |
| 磁盘/健康 | 真实磁盘统计、健康度、诊断动作 |

---

## 12. 交付与同步

- 本计划为 V5 验收基线；QA 随契约先行出可执行用例，逐批次维护用例清单（含 BOOT-01~10 与 §5–§8 各表）。
- 用例文档归档于 `reports/`，与 `docs/api-contract.md` 保持一致；契约或设计变更时同步更新。
- 每批冒烟/回归结论落 develop 频道，进度概览落 project 频道。
- 待 PrePan 审阅本计划后，QA 按 §10 门槛逐批执行。
