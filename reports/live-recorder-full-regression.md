# Live Recorder 全量回归计划（2026-08-28 本轮）

> 依据：PrePan 指示按行业标准测试流程，全面测试并回归所有问题后通知其验证。
> 前置：BE fix 分支完成 #41/#42 → QA 分支回归 → 合入 main → QA main 全量回归 → 通知 PrePan。

## 1. 回归范围（按 AC + 缺陷拆分）

### A. 预览链路（#41 新增，重点）
| # | 场景 | 通过标准 |
|---|---|---|
| A1 | 录制一开始即连预览 | WS 首块含 FLV 头 `464c56`，mpegts 可出画 |
| A2 | **录制中途加入预览（PrePan 复现场景）** | 新客户端收到补发的 FLV 头+metadata+SPS/PPS，可出画，无 Non MPEG-TS/FLV |
| A3 | 预览 WS 直连后端 + 经 5173 代理 | 两种路径均握手 OPEN、出流 |
| A4 | 非录制房间预览 | 4002 PREVIEW_NOT_RECORDING |
| A5 | 第 3 路预览超限 | 4003，第 3 路被拒 |
| A6 | 停止录制/断流 | WS 收 stream_end，关闭码 1000/4004 |

### B. 录制计数（#42 新增，重点）
| # | 场景 | 通过标准 |
|---|---|---|
| B1 | 录制启动 | 收到 service:status SSE，activeRecordings 0→1 |
| B2 | 录制完成/停止 | activeRecordings 1→0 |
| B3 | 顶部导航「录制中 N」 | 页面开启状态下的录制启停实时更新，不依赖刷新 |

### C. 存量功能回归
| # | 场景 | 通过标准 |
|---|---|---|
| C1 | 收藏（#37） | PATCH 200/404、true↔false 持久化、GET/SSE 带 favorited |
| C2 | 收藏置顶（#38） | 监控总览 favorited 置顶排序 |
| C3 | 录制时长（#35/#36） | activeRecording{recordingId,startedAt} 录制中就位、停止复位 null；时长秒级走时 |
| C4 | SSE enrich（#40） | 录制中收藏/编辑，SSE room:updated 仍带 activeRecording，时长不消失 |
| C5 | 幂等迁移 v3（#39） | 全新库 1→2→3、缺列库补列、幂等 0 新增 |
| C6 | 手动停止后重录（#33） | 录制→停止→再检测→同场重录 |
| C7 | 删除/启停房间实时同步（#29） | 删除即列表移除、disabled 事件 |
| C8 | 检测性能（#27/#28） | 立即检测 200、耗时 <1s、并发不重复 |
| C9 | 预览数据源类型（#31） | mpegts type=flv + ws:// 正确 |
| C10 | 磁盘保护/并发上限 | 阈值触发拒绝+告警 |

### D. 异常与稳定性
| # | 场景 | 通过标准 |
|---|---|---|
| D1 | 录制频繁启停连续性 | 观察单场录制时长分布，确认无系统性短录（区分 B站 会话特性 vs 缺陷） |
| D2 | 断流重连 | 5/15/45s 退避、耗尽 failed 保留已录 |
| D3 | 全量测试 | 后端 vitest 全绿、前端 build/lint |
| D4 | 服务重启 | 迁移幂等、任务恢复、on-disk 库 schema 正确 |

## 2. 执行方式
- 分支回归：#41/#42 提测后，在 fix 分支跑对应用例 + 全量测试
- main 复测：合入后按 A–D 全量执行
- 工具：curl/ws 脚本实测 API+SSE+WS；无浏览器自动化，可视出画项需 PrePan 确认

## 3. 输出
- 回归结果记录（本文件追加）
- 全部通过后由 @QA 通知 PrePan 验证