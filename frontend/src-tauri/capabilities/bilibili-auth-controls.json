{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "bilibili-auth-controls",
  "description": "Only the local confirmation bar in the Bilibili authorization window may invoke app commands.",
  "webviews": ["bilibili-auth-controls"],
  "permissions": ["core:default"]
}
